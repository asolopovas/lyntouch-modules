{% if  fields.start_breakpoint  %}
    <style scoped>
        .swiper-wrapper {
            flex-direction: column;
        }

        @media only screen and (min-width: {{ fields.start_breakpoint }}px) {
            .swiper-wrapper {
                flex-direction: row;
            }
        }
    </style>
{% endif %}
